# Changelog

## v2.2.78 - 2026-07-14-1821

- Added a DEV002 setup console below the HW/SW controls.
- Added device-specific setup console logs via `data/console-dev001.log` and `data/console-dev002.log`.
- Updated setup consoles to show general console output plus only the matching device-specific setup output.

## v2.2.77 - 2026-07-14-1814

- Hid DEV001-only setup/audio and setup/odometer controls on non-DEV001 setup pages so DEV002 no longer shows them over HW/SW controls.

## v2.2.76 - 2026-07-14-1809

- Hid Style and System sliders on the Themes page when all buttons fit without scrolling.
- Centered the visible main menu button row so the reduced menu no longer starts too far left.

## v2.2.75 - 2026-07-14-1803

- Removed the Boot entry from the main menu while keeping the Boot page as the internal startup screen.
- Removed unused reserve menu entries and deleted the reserve page modules.
- Updated main menu page mapping so the visible menu starts at Dash and no longer opens at offset position 2.
- Hid the main menu scrollbar when all menu buttons fit on screen.

## v2.2.74 - 2026-07-14-1738

- Matched the DEV001 setup subpage button font to the smaller odometer keypad font.
- Reduced the DEV001 odometer keypad font size for cleaner button text.
- Fixed the DEV001 setup/audio quicksound list scroll callback used when opening the audio subpage.

## v2.2.73 - 2026-07-14-1732

- Removed the green accent from DEV001 setup subpage buttons and the quicksound selection highlight.
- Added Linux launcher permission repair after `update.py` release update/install extraction.

## v2.2.72 - 2026-07-14-1728

- Removed the always-green `sys_clr[10]` accent from DEV001 setup/audio buttons and scroll controls.
- Rebuilt quicksound setup buttons as theme-colored label buttons so their colors render reliably.
- Replaced the native quicksound scrollbar with a theme-colored vertical slider.

## v2.2.71 - 2026-07-14-1724

- Constrained the DEV001 `AUDIO` setup content to the left half of the setup page to keep right-side reserve space free.
- Switched quicksound setup buttons to the same `Bebas Neue Bold` font family as the HW/SW buttons.
- Replaced the quicksound selection scrollbar with a themed Tk scrollbar so theme colors apply more reliably.

## v2.2.70 - 2026-07-14-1718

- Enlarged the DEV001 `AUDIO` setup subpage for better touch operation.
- Increased quicksound button, list, scrollbar, and label sizes.
- Applied consistent theme colors and highlight handling to the new setup/audio controls.

## v2.2.69 - 2026-07-14-1702

- Added DEV001 setup subpages for `HW/SW`, `AUDIO`, and `ODOMETER`.
- Moved DEV001 HW/SW buttons and favorite toggles into the `HW/SW` setup subpage.
- Moved DEV001 quicksound controls into the `AUDIO` setup subpage.
- Moved DEV001 odometer/config selectors and numpad into the `ODOMETER` setup subpage.

## v2.2.68 - 2026-07-14-1647

- Fixed GPS odometer distance timing so RMC distance uses the previous valid GPS cycle instead of resetting the timer every read.
- Kept Raspberry Pi power-off safety by writing each valid GPS distance increment directly to saved trip and total odometer values.
- Fixed GPS trip reset so the in-memory counters and displayed trip values are cleared immediately.
- Changed GPS odometer runtime counters to start at zero instead of one.

## v2.2.67 - 2026-07-14-1632

- Reduced and shifted the Dash GPS reception label so it ends shortly after the satellite count and stays within the red panel.

## v2.2.66 - 2026-07-14-1618

- Moved the Dash GPS reception bars into their own fixed-width label next to the GPS time field.
- Kept the GPS time label stable so the reception display no longer shifts with changing seconds.

## v2.2.65 - 2026-07-14-1614

- Replaced the Dash GPS `Sxx Fq` text with a phone-style ASCII reception bar display.
- Kept the satellite count next to the GPS reception bars for quick diagnostics.

## v2.2.64 - 2026-07-14-1611

- Added GPS satellite count and fix-quality state from GGA parsing.
- Displayed compact GPS reception status after the Dash GPS time as `Sxx Fq`.

## v2.2.63 - 2026-07-14-1603

- Published parsed GPS values back to the shared app context so Dash GPS fields update from GGA/RMC reads.
- Initialized the GPS save flag before parsing so GPS odometer updates cannot reference it before assignment.

## v2.2.62 - 2026-07-14-1558

- Marked `start.sh` executable in Git so the Raspberry Pi systemd service can launch KIDD without `Permission denied`.
- Marked `install.sh` executable in Git for direct installer use on Linux/Raspberry Pi.

## v2.2.61 - 2026-07-14-1554

- Added persistent console logging to `data/console.log` so Setup console output can be inspected after runtime.
- Limited the console log file size and ignored the runtime log file in Git.

## v2.2.60 - 2026-07-14-1551

- Cached Setup HW/SW/FAV button render states so the periodic Setup refresh only touches widgets when their visual state changes.
- Reduced visible Setup flicker after HW/SW button presses by avoiding redundant Tkinter `config()` calls.

## v2.2.59 - 2026-07-14-1549

- Added a DEV001 Setup GPS diagnostic poll so the Setup console shows whether NMEA data is arriving while GPS HW is enabled.
- Logged throttled GPS RMC/GGA success details including time, position, altitude, satellites, and fix quality.
- Split GPS diagnostic throttling by message type so no-data, no-fix, and valid-fix messages do not suppress each other.

## v2.2.58 - 2026-07-14-1547

- Removed Setup HW/SW button globals and stored those widgets on the Setup frame instance.
- Updated Setup HW/SW buttons live after toggles instead of reloading the whole Setup page.
- Refreshed the Setup console immediately after HW/SW toggles so new log lines appear without waiting for a page rebuild.

## v2.2.57 - 2026-07-14-1545

- Made Setup HW/SW console logging independent from direct global text-list access.
- Added a fallback log path so button toggles still write to the Setup console when a label lookup fails.

## v2.2.56 - 2026-07-14-1541

- Fixed Setup HW/SW toggles so console logging cannot prevent state publishing.
- Made Setup console button log labels tolerant when a device text list is missing or shorter than expected.

## v2.2.55 - 2026-07-14-1536

- Added direct Setup console log entries when the DEV001 console opens.
- Logged HW and SW setup button toggles directly to the console buffer so state changes are visible even without terminal output.

## v2.2.54 - 2026-07-14-1534

- Restored the MP3 ignore rule so audio files are no longer added to Git.
- Removed already tracked MP3 files from the Git index while keeping the local files in place.

## v2.2.53 - 2026-07-14-1530

- Fixed version loading so `version.txt` is read from the project root again.
- Restored the Setup page version line with explicit version, change date, platform, car number, device number, and IP text.

## v2.2.52 - 2026-07-14-1513

- Added a DEV001 Setup console panel in the existing right-side frame that mirrors recent console output.
- Captured stdout and stderr in a lightweight ring buffer while preserving normal terminal output.
- Added throttled GPS diagnostics for missing NMEA data, invalid NMEA lines, and missing GPS fix/position data.

## v2.2.51 - 2026-07-12-1702

- Added DEV002 `K2_S05` value labels next to the first nine PB buttons so each powerboard display shows its current value directly beside the button.
- Adjusted the DEV002 `K2_S05` PB value labels 10px to the right and 5px upward.

## v2.2.50 - 2026-07-12-1656

- Fixed DEV002 LED scaling so RPM and other gauges map their real value ranges to the available LED count instead of using placeholder quantities.
- Applied the corrected value-to-LED scaling to DEV002 RPM, main gauges, and AUX gauges.

## v2.2.49 - 2026-07-12-1651

- Replaced the coarse DEV002 frame limiter with phased gauge rendering so DEV002 values stay responsive while heavy image groups are spread across frames.

## v2.2.48 - 2026-07-12-1648

- Increased the DEV002 dashboard gauge frame limiter so heavy image updates run less often on Raspberry Pi.

## v2.2.47 - 2026-07-12-1645

- Skipped dashboard QuickSound widget creation entirely when the active device/theme has no QuickSound button positions, such as DEV002.

## v2.2.46 - 2026-07-12-1645

- Added a DEV002 dashboard frame limiter so heavy gauge image updates are capped and cached between fast update cycles.

## v2.2.45 - 2026-07-12-1640

- Ignored void/no-fix GPS NMEA sentences without raising timestamp errors when GPS has no reception.
- Reduced GPS journal spam by silently skipping empty, invalid, or incomplete NMEA data.

## v2.2.44 - 2026-07-12-1634

- Reduced dashboard QuickSound label updates by caching rendered text and scrolling labels at a fixed slower interval.
- Cached AUTOPLAY QuickSound button image updates to avoid redundant Tkinter image configuration in the fast dashboard loop.

## v2.2.43 - 2026-07-12-1627

- Reduced dashboard system-data refresh frequency to avoid frequent Raspberry Pi stalls from hardware/system probes.
- Added lightweight slow-update logging to show which dashboard section causes frame hitches in the systemd journal.

## v2.2.42 - 2026-07-12-1626

- Skipped GPS reads while DEV001 simulation mode is active so simulated gauges do not block on serial input.
- Changed the GPS serial port to non-blocking mode after detection to prevent `readline()` from stalling the dashboard update loop.

## v2.2.41 - 2026-07-12-1621

- Moved Raspberry Pi system data collection off the Tkinter update loop so psutil/vcgencmd refreshes no longer stall the dashboard once per second.
- Added a short timeout for `vcgencmd measure_temp` and kept the psutil temperature fallback.

## v2.2.40 - 2026-07-12-1614

- Published refreshed Raspberry Pi system data back to the dashboard context so psutil values replace the default `88...` placeholders.
- Updated DEV001 sysinfo refresh to collect Linux system data without requiring the function toggle first.
- Added a CPU temperature fallback through `psutil.sensors_temperatures()` when `vcgencmd` returns no value.

## v2.2.39 - 2026-07-12-1610

- Made Setup page WLAN IP display tolerant when `wlan0` has no IPv4 address or the `ip` command cannot return interface data.

## v2.2.38 - 2026-07-12-1600

- Updated `install.sh` to install bundled KIDD `.ttf` and `.otf` fonts into the current user's Linux font directory.
- Added `fontconfig` to Linux/Pi system packages so the font cache can be refreshed during installation.

## v2.2.37 - 2026-07-12-1556

- Made Linux hostname parsing tolerant when the Raspberry Pi hostname does not contain a `dev` marker.
- Added regression coverage for runtime device-name parsing fallbacks.

## v2.2.36 - 2026-07-12-1554

- Added missing Raspberry Pi dependency `psutil` to `requirements-rpi.txt`.
- Added installer import checks so missing Python modules are reported during installation before KIDD starts.

## v2.2.35 - 2026-07-12-1551

- Added `start.sh` so KIDD starts with the local `.venv` Python and finds installed modules such as `pynmea2`.
- Updated the Raspberry Pi autostart service to launch KIDD through `start.sh`.

## v2.2.34 - 2026-07-12-1548

- Removed the `.gitignore` rule that excluded MP3 files so the `sound` folder contents can be tracked.

## v2.2.33 - 2026-07-12-1545

- Added Raspberry Pi autostart setup to `install.sh` using a `kidd.service` systemd service for `main.py`.
- Added `--no-autostart` to skip service creation when installing dependencies only.
- Added display/session environment values to the Raspberry Pi autostart service for GUI and audio startup.

## v2.2.32 - 2026-07-12-1542

- Added `install.sh` for installing KIDD system packages and Python modules on Linux and Raspberry Pi.
- Added installer options for Raspberry Pi hardware modules, optional audio dependencies, custom Python executables, and local virtual environments.
- Changed `install.sh` to use a local `.venv` by default so current Raspberry Pi OS/Debian installations do not hit the externally managed Python environment error.

## v2.2.31 - 2026-07-12-1529

- Added an updater button for a complete KIDD installation from GitHub into the current user's `KIDD` folder on Windows and Raspberry Pi.
- Added install-folder backup handling before a complete install replaces an existing user `KIDD` folder.

## v2.2.30 - 2026-07-12-1527

- Moved `update.py` back into the project root.
- Updated the Visual Studio project entry for the root-level updater.
- Reworked the updater to use an explicit app root, remove the global sound checkbox state, add request timeouts, and extract release ZIP files with path safety checks.
- Fixed Windows GUI updates so Tkinter widget options like `state` and `text` are passed correctly from the updater worker thread.

## v2.2.29 - 2026-07-08-1051

- Added `AUTOPLAY` as a QuickSound button mode.
- Allowed only one QuickSound button to use `AUTOPLAY` at a time.
- Updated dashboard QuickSound buttons so an `AUTOPLAY` button toggles Audio Autoplay and reflects its ON/OFF state.

## v2.2.28 - 2026-07-08-1046

- Restored Audio page Autoplay controls for random MP3 playback.
- Added current track, next track, and countdown display for Autoplay.
- Reworked Autoplay to use `pygame.mixer.music` with shuffled playlists from the active sound folder.
- Stopped Autoplay automatically when a manual MP3 button is played.
- Changed Autoplay button text to `AUTO START` / `AUTO STOP`.
- Added a 30 second pause between Autoplay tracks.
- Reduced Audio Autoplay display label sizes to avoid overlapping headers and frame borders.
- Updated Audio Autoplay label colors to use the active system theme colors.
- Changed Audio Autoplay countdown to whole seconds and reduced the countdown label size.
- Hid `.mp3` extensions from visible Audio and QuickSound file labels while keeping stored filenames unchanged.

## v2.2.27 - 2026-07-08-0949

- Added a Setup toggle for showing or hiding only the four dashboard QuickSound labels.
- Stored QuickSound label visibility in `quicksound_settings`.
- Updated dashboard QuickSound rendering to skip only QuickSound labels when disabled.

## v2.2.26 - 2026-07-08-0907

- Added Setup dropdowns to choose the QuickSound subfolder and MP3 file per dashboard sound button.
- Added QuickSound helpers for listing available sound folders and MP3 files from the active sound set.
- Removed the extra QuickSound filename label from Setup.
- Replaced QuickSound sound selectors with themed popup lists capped at 600px height.
- Changed QuickSound selector popups to embedded Setup listboxes so dropdowns open reliably on the dashboard display.
- Removed QuickSound dropdowns and added a fixed-size selection list beside the QuickSound controls.
- Enlarged QuickSound select list rows and restyled the scrollbar for touchscreen use.
- Moved QuickSound section titles upward and applied a dedicated themed scrollbar style.

## v2.2.25 - 2026-07-08-0843

- Added persistent QuickSound setup entries for the four dashboard sound buttons.
- Added Setup controls to switch each QuickSound button between LOOP and 1X playback.
- Added Setup controls to switch each QuickSound button color between YE, GN, and RD.
- Updated dashboard QuickSound buttons to use their configured mode and color.
- Expanded QuickSound color selection to AQ, BU, GN, OR, RD, WH, and YE.

## v2.2.24 - 2026-07-08-0831

- Moved QuickSound labels below their dashboard buttons.
- Changed QuickSound labels to use the smaller setup/HW label style.
- Placed QuickSound labels exactly 1px below their dashboard buttons.
- Limited QuickSound label width to the actual button image width.

## v2.2.23 - 2026-07-08-0827

- Added labels above the four dashboard QuickSound buttons.
- Added marquee scrolling for long QuickSound labels so long filenames remain readable.

## v2.2.22 - 2026-07-08-0823

- Reverted the QuickSound fallback resolver and restored the original explicit QuickSound filenames.
- Verified the reported KITT2000 `notouch` and `introduce` MP3 files exist at the exact paths used by the app.

## v2.2.21 - 2026-07-08-0803

- Added QuickSound filename fallback resolution so dashboard sound buttons use existing files across sound folders.
- Replaced hardcoded missing QuickSound filenames with candidate lists for `notouch` and `introduce` sounds.

## v2.2.20 - 2026-07-07-1537

- Cleaned up the project root by moving reusable helpers into `functions/` and setup/platform modules into `setup/`.
- Updated imports and `KIDD.pyproj` compile entries for the new folder structure.
- Updated README project layout documentation for the new folders.

## v2.2.19 - 2026-07-07-1529

- Moved QOPT favorite mapping, labeling, status, and target resolution into `favorite_manager.py`.
- Added regression tests for device-specific favorite limits, DEV002 relay favorites, source status text, and original action mapping.

## v2.2.18 - 2026-07-07-1523

- Limited QOPT favorite discovery to the active device's valid favorite range so DEV002 relay favorites are not shown on DEV001.
- Synchronized all button state lists when switching devices from the theme/setup controls.

## v2.2.17 - 2026-07-07-1520

- Fixed Setup favorite buttons reverting after being disabled by synchronizing the page-local favorite state immediately after toggles.

## v2.2.16 - 2026-07-07-1518

- Fixed first-click Setup favorite button recoloring by using the new state returned from the controller toggle.
- Published the loaded sound folder and sound file lists back to the shared context for Quicksound button setup.

## v2.2.15 - 2026-07-07-1514

- Fixed QOPT status text lookup when `states_txt_act` is not present in the controller namespace.
- Updated Setup favorite buttons to recolor immediately when toggled.

## v2.2.14 - 2026-07-07-1511

- Fixed QOPT favorite mapping so it no longer depends on Setup page `quant_btns_*` globals.
- Updated Setup favorite `F` buttons to use an active background color when enabled.

## v2.2.13 - 2026-07-07-1508

- Added QOPT favorite mapping so active setup `F` buttons populate the next free QOPT slots.
- QOPT slots now show the source function label above the button and the current status on the button.
- QOPT favorite buttons now trigger the original HW, SW, or DEV002 relay function.

## v2.2.12 - 2026-07-07-1504

- Fixed QOPT page state after controller extraction by publishing generated QOPT buttons and count back to the shared page context.

## v2.2.11 - 2026-07-07-1501

- Fixed DEV002 sysinfo meter updates by ensuring the secondary 7-segment label exists before updating it.

## v2.2.10 - 2026-07-07-1459

- Fixed button callbacks after controller extraction by publishing changed button and relay state back to the shared page context.

## v2.2.9 - 2026-07-07-1454

- Published the active style color palette back to the main page context after style changes.

## v2.2.8 - 2026-07-07-1454

- Moved the former `myfunctions` controller out of `main.py` into `app_controller.py`.
- Replaced direct controller references to `kidd` and `read` with `self.app` and `self`.
- Refreshed page context after controller creation and page imports.
- Converted navigation button lists and counters from module globals to controller instance state.
- Reduced `main.py` from 3921 lines to 3133 lines.

## v2.2.7 - 2026-07-07-1448

- Added dependency files for base, optional audio, and Raspberry Pi hardware installs.
- Reworked README installation, run, test, and project layout documentation.
- Fixed GPS odometer persistence so values are written to `odo_config`.
- Removed stale odometer data from the generic `config` section.
- Added smoke tests for platform detection, config/text/style loading, and odometer persistence.
